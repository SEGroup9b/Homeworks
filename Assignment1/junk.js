    /*var read_stream = fs.createReadStream( 'listings.json' );
    read_stream.on( 'data', writeCallback );
    read_stream.on( 'close', closeCallback );

    function writeCallback( listingData ){
      response.write( listingData );
    }
    function closeCallback(){
      response.end();
    }
    */